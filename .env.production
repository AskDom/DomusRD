# CRA carga esto automáticamente en "npm run build". Sin sourcemaps en el
# build de producción, el código fuente original no queda reconstruible
# desde el bundle servido en el navegador.
GENERATE_SOURCEMAP=false
